<h1>New link</h1>

<%= error_messages_for :link %>

<% form_for(@link) do |f| %>
	<%= render :partial => 'form', :locals => {:f => f} %>

  <p>
    <%= f.submit "Create" %>
  </p>
<% end %>

<%= link_to 'Back', links_path %>
